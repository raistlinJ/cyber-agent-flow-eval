For supplied service observations, return only a JSON object with a `services`
array. Each entry should contain `host`, numeric `port`, and `protocol`.
Copy observations faithfully; do not infer additional services.
